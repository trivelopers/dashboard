# Frontend – Chatbot Admin Panel

Este proyecto corre el frontend (Vite + React) dentro de un contenedor Docker para desarrollo.

## 🚀 Levantar entorno

Desde la raíz del proyecto:

```bash
docker compose -f docker-compose.dev.yml up --build -d
